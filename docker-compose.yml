version: '3.8'
services:
  easyview:
    build: .
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/server/node_modules
    ports:
      - "5173:5173"
      - "4000:4000"
    env_file:
      - .env
    environment:
      - CHOKIDAR_USEPOLLING=true

